name: Test And Deploy Dev
on:
  push:
    branches:
      - dev

jobs:
  test:
    uses: ./.github/workflows/test.yml


  deploy:
    environment: Dev
    needs: test
    uses: ./.github/workflows/deploy-dev.yml
    secrets: inherit
