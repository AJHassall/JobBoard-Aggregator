name: Test And Deploy Dev
on:
  push:
    branches:
      - dev

jobs:
  test:
    uses: ./.github/workflows/test.yml


  deploy:
    needs: test
    environment: Dev
    uses: ./.github/workflows/deploy-dev.yml
    secrets: inherit
