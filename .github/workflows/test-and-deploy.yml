name: Test And Deploy Dev
on:
  push:
    branches:
      - dev

jobs:
  test:
    runs-on: self-hosted
    environment: Dev
    uses: ./.github/workflows/test.yml
  deploy:
    runs-on: self-hosted
    environment: Dev
    needs: test
    uses: ./.github/workflows/deploy-dev.yml
